<%-
values = []
@conf_values.each_pair do |key,value|
  next if (value.nil? || value == 'UNSET')
  if value.is_a?(Array)
    values << "#{key}=#{value.join(',')}"
  else
    values << "#{key}=#{value}"
  end
end
-%>
<%= values.join(" ") %>
