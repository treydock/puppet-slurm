<%
values = []
values << (@required ? "required" : "optional")
values << @plugin
case @arguments
when Hash
  @arguments.each_pair do |k,v|
    values << "#{k}=#{v}"
  end
when Array
  @arguments.each do |a|
    values << a
  end
when String
  values << @arguments
end
-%>
<%= values.join(" ") %>
